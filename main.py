from datetime import datetime
from collections import defaultdict 

def get_birthdays_per_week(users):
  birthdays_sorted = defaultdict(list)
  for k, v in users:
    birthdays_sorted[]
  
  current_date = datetime.today().date()
  print(datetime(1955, 10, 28))

get_birthdays_per_week([{"name": "Bill Gates", "birthday": datetime(1955, 10, 28)}, {"name": "Bill", "birthday": datetime(1925, 12, 22)}])